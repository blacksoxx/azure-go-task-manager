apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shared-configs
  namespace: argocd
  labels:
    app: shared-configs
    environment: production
spec:
  project: default
  source:
    repoURL: https://github.com/blacksoxx/azure-go-task-manager.git  # ⚠️ Update this!
    targetRevision: HEAD
    path: k8s/overlays/production/shared-configs
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true