apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

bases:
- ../../../base/auth-service

# ✅ Include shared staging configs
resources:
- ../shared-configs

patchesStrategicMerge:
- replica-patch.yaml

images:
- name: auth-service
  newTag: latest